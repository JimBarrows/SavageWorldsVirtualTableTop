  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
	<a class="navbar-brand" href="#">Savage Worlds</a>
      </div>
      <div class="navbar-collapse collapse">
	<ul class="nav navbar-nav">
	  {{#if session.isAuthenticated}}
            <li>{{#link-to 'member'}}Home{{/link-to}}</li>
	    <li>{{#link-to 'plot-points' }}Plot Points{{/link-to}}</li>
	  {{/if}}
	</ul>
	<ul  class="navbar-right">
	  {{#if session.isAuthenticated}}
            <a {{ action 'invalidateSession' }} class="btn btn-danger">Logout</a>
	  {{else}}
	    {{#link-to 'login' class="btn btn-success "}}Login{{/link-to}}
	    {{#link-to 'signup' class="btn btn-default "}}Sign up{{/link-to}}
	  {{/if}}
	</ul>
      </div>
    </div>
  </nav>
  {{outlet}}
