import {
  hindranceAvailable
} from 'gamemaster/helpers/hindrance-available';

module('HindranceAvailableHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = hindranceAvailable(42);
  ok(result);
});
