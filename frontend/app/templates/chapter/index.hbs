<h2>
	<div class="btn-group" role="group">
		{{#link-to 'chapter.edit' chapter class="btn btn-default btn-xs"}}
			<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
		{{/link-to}}
		<button {{action "remove" chapter}} class="btn btn-danger btn-xs">
			<span class="glyphicon glyphicon-remove-sign" aria-hidden="true"></span>
		</button>
	</div>
	{{model.name}}
</h2>
{{{model.description}}}
